extend = "../pyproject.toml"

line-length = 95

[lint.isort]
section-order = [
  "future",
  "testing",
  "standard-library",
  "third-party",
  "first-party",
  "local-folder",
]
known-first-party = ["piculet"]

[lint.isort.sections]
testing = ["pytest"]
